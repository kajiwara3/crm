<table class="table table-striped table-hover">
  <thead>
    <tr>
      <th>name</th>
    </tr>
  </thead>
  <tbody>
    <% @administrators.each do |admin| %>
      <tr>
        <td><%= admin.name %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<%= will_paginate @members %>
